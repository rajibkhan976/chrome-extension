import React, {useEffect} from 'react';
import { useNavigate } from 'react-router-dom';

const Home = (props) => {
  
    return (
        <div>
           Home 
        </div>
    );
};

export default Home;